#pragma once

#include <iostream>
#include <print>

#include "common.hpp"

using Value = double;

/**
 * Print to the standard output a Value
 * @param value: Value to print
 */
void printValue(Value value);
